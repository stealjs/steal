jQuery.Test.Unit.extend("Tests.Unit.<%=name%>",{
    init : function(){ //setup code
        
    },
    destroy : function(){ //teardown code
        
    },
    test_truth: function() {
       this.assert(true)
    }
});